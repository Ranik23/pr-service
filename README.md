# Avito Tech Assignment

Сервис для управления код-ревью в командах.

## Запуск

```bash
docker compose up
```

## Env-переменные

SERVER_PORT - отвечает за порт, куда будет проброшен сервер

SWAGGER_PORT - за порт, куда будет проброшен сваггер

Остальные параметры больше для локального запуска, тк они и так уже прописаны в докер файле компоуза

## Доступ
- Swagger UI: http://localhost:${SWAGGER_PORT} - документация и тестирование API
- Сервер: http://localhost:${SERVER_PORT}

##

Изначально я думал, что один юзер может быть в нескольких командах. Поэтому у меня есть связывающая таблица "UserTeam". Но перечитав условия, я решил, что это не так (возможно я не прав).


## Стресс-тест

В стресс-тесте просто описан один сценарий
![Photo](./docs/a.png)
